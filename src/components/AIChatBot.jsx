import React from "react";

function AIChatBot() {
  return (
    <div
      style={{
        position: "fixed",
        bottom: "20px",
        right: "20px",
        background: "#111827",
        color: "white",
        padding: "12px 16px",
        borderRadius: "10px",
        fontSize: "12px"
      }}
    >
      🤖 AI ChatBot disabled (API key not configured)
    </div>
  );
}

export default AIChatBot;
